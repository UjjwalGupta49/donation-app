import { Button } from '@nextui-org/react';

export const CreateDonationCampaign = () => {
    return (
        <div>
            <Button bordered color="gradient" auto>
                Create donation campaign
            </Button>
        </div>
    );
};
